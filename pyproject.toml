[project]
name = "docker-radicale"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.4.0",
    "pytest>=8.3.5",
    "pytest-testinfra>=10.2.2",
    "ruff>=0.14.5",
]
